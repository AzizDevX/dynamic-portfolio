{
  "name": "portfolio-app",
  "version": "1.1.0",
  "description": "Full-stack portfolio application",
  "scripts": {
    "install-pack": "npm install && concurrently \"cd backend && npm install\" \"cd frontend && npm install\"",
    "build": "cd frontend && npx vite build",
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm run start-backend",
    "start:frontend": "npm run wait && cd frontend && npm run start-frontend",
    "wait": "node -e \"setTimeout(() => process.exit(0), 3500)\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
